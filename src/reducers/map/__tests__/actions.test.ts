import { setMap } from '../actions';
import {
  MAP_SET, MAP_INIT, INSTANCE_SET,
  IS_LOADED, MAP_RESET, LAYER_SET, MODE_SET,
  STEP_SET, STEP_MINUS, STEP_ADD,
  GEOMETRY_GET, GEOMETRY_SET,
} from '../../../constants/action-types';

// describe('actions', () => {
//   it('should create an action to toggle the mode', () => {
//     const expectedAction = {
//       type: MAP_SET,
//     };
//     expect(setMap()).toEqual(expectedAction);
//   });
// });
